数据中台  -业务架构
数据中台  -技术架构

技术架构知识点：
1.Hadoop 是一个处理、存储和分析海量的分布式、非结构化数据的开源框架
		 分析和处理大数据的软件平台，是一个用 Java 语言实现的 Apache 的开源软件框架,在大量计算机组成的集群中实现了对海量数据的分布式计算。
		 Hadoop 采用 MapReduce 分布式计算框架，根据 GFS 原理开发了 HDFS（分布式文件系统），并根据 BigTable 原理开发了 HBase 数据存储系统。
		 
2.Hadoop 的功能，
			数据管理系统，作为数据分析的核心，汇集了结构化和非结构化的数据
			
			大规模并行处理框架，拥有超级计算能力
			
			幵源社区，为解决大数据的问题提供工具和软件
		 
		 
总结：Hadoop 生态圈，这些组件包括数据存储、数据集成、数据处理和其他进行数据分析的专门工具。
Hadoop 的生态系统，主要由 HDFS、MapReduce， HBase， Zookeeper， Pig、 Hive 等核心组件构成，另外还包括 Sqoop、Flume 等框架，用来与其他企业系统融合。

3.Hadoop生态系统的核心组件

https://www.cnblogs.com/qinjf/p/8390884.html
3.1 HDFS：即Hadoop分布式文件系统（Hadoop Distributed Filesystem），以流式数据访问模式来存储超大文件	
		HDFS数据块：HDFS上的文件被划分为块大小的多个分块，作为独立的存储单元，称为数据块，默认大小是64MB
		HDFS的三个节点：Namenode，Datanode，Secondary Namenode
			Namenode：HDFS的守护进程，用来管理文件系统的命名空间，负责记录文件是如何分割成数据块，以及这些数据块分别被存储到那些数据节点上，它的主要功能是对内存及IO进行集中管理。
			Datanode：文件系统的工作节点，根据需要存储和检索数据块，并且定期向namenode发送他们所存储的块的列表。
			Secondary Namenode：辅助后台程序，与NameNode进行通信，以便定期保存HDFS元数据的快照。
			
https://zhuanlan.zhihu.com/p/82399103			
3.2 MapReduce分布式计算框架，Map和Reduce其实是两种操作。
		Map（映射）: 把人的统一的步骤进行标准程序化
		Reduce（化简）:
		MapReduce本质上就是方法三，但是如何拆分文件集，如何copy程序，如何整合结果这些都是框架定义好的。我们只要定义好这个任务（用户程序），其它都交给MapReduce。
		
		一是软件框架，二是并行处理，三是可靠且容错，四是大规模集群，五是海量数据集。
		MapReduce的思想就是“分而治之”。
		
		一个比较形象的语言解释MapReduce：　　
		我们要数图书馆中的所有书。你数1号书架，我数2号书架。这就是“Map”。我们人越多，数书就更快。
		现在我们到一起，把所有人的统计数加在一起。这就是“Reduce”。
		
		一个MapReduce作业通常会把输入的数据集切分为若干独立的数据块，由Map任务以完全并行的方式去处理它们。
		框架会对Map的输出先进行排序，然后把结果输入给Reduce任务。通常作业的输入和输出都会被存储在文件系统中，整个框架负责任务的调度和监控，以及重新执行已经关闭的任务。


3.3	HBase 是一个高可靠性、高性能、面向列、可伸缩类型的分布式存储系统
		HBase位于Hadoop的结构化存储层，Hadoop HDFS为HBase提供了高可靠性的底层存储支持，Hadoop MapReduce为HBase提供了高性能的计算能力，Zookeeper为HBase提供了稳定服务和failover机制。
		一个可扩展的分布式数据库，支持大表的结构化数据存储。是一个建立在 HDFS 之上的，面向列的 NoSQL 数据库，用于快速读/写大量数据。
		
3.4 Zookeeper  是一个分布式的，开放源码的分布式应用程序协调服务，是Hadoop和Hbase的重要组件
		提供的功能包括：配置维护、域名服务、分布式同步、组服务等。
		
		
3.5 Hive一个建立在 Hadoop 上的数据仓库基础构架，将结构化的数据文件映射为一张表，类似于RDBMS(关系型数据库，如MySQL、Oracle、PgSQL)，并提供类SQL的查询功能。
		Hive是一个将SQL转换为MR任务的工具，进行数据提取转化加载（ETL）
		
3.6 Yarn 资源调度和管理框架，
		
3.7 Mahout 可扩展的机器学习和数据挖掘库。它提供的 MapReduce 包含很多实现方法，包括聚类算法、回归测试、统计建模。


3.8 Amban 一个基于 Web 的工具，用来供应、管理和监测 Hadoop 集群，也提供了一个可视的仪表盘来查看集群的健康状态

4. 关键问题

4.1 大规模计算机集群上要处理好故障问题

4.2 实时性较差，由于HDFS存储底层都是在磁盘中进行的，以及原生的MapReduce的中间结果也都要存储在磁盘上，所以 Hadoop 的实时性不太好。

5.其他重要概念

元数据：
		数据字典：描述数据的结构信息，表结构信息【表名、字段名称、类型、注释、表的数据产出任务、表和字段的权限】
				规范的，无二义性的语言表达数据流程图的各组成部分
				
				例如  样例
					
					数据项条目（不可再分的数据单位）
				数据项名称：车站代码
				别名：Coach stationID
				类型长度：char(3)
				描述：由三位字母组成车站编码
				
		数据结构：反映数据之间的组合关系
				例如  样例
					
					数据结构条目
				数据结构名称：前台用户
				含义说明：是前台应用层面的主体数据结构，定义了前台用户的相关信息
				组成：用户编号、用户名、密码、性别、注册时间、E_mail、真实姓名、身份证号码、联系电话
				
		数据血缘：一个表由哪些表加工过来的。数据血缘关系包含了集群血缘关系、系统血缘关系、表级血缘关系和字段血缘关系，其指向数据的上游来源，向上游追根溯源。
					数据血缘可以帮我们做影响分析和故障溯源。可以解决当数据发生变化时通知下游端进行调整。
					https://cloud.tencent.com/developer/article/1987361
					图数据库是基于图论实现的新型数据库，擅长处理点和边组成的复杂关系网络，执行和查询效率较传统关系型数据库具有无可比拟的优势。  
				
				看起来像   桑基图
				